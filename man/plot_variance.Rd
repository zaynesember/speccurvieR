% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{plot_variance}
\alias{plot_variance}
\title{Plot the variance decomposition of a specification curve}
\usage{
plot_variance(
  sca_data,
  estimate = "coef",
  method = c("lmg", "type2"),
  residual = TRUE,
  title = ""
)
}
\arguments{
\item{sca_data}{A data frame returned by [sca()].}

\item{estimate}{A string naming the numeric column to decompose. Defaults to
`"coef"` (the focal coefficient).}

\item{method}{One of `"lmg"` (default; LMG/Shapley decomposition of
R-squared) or `"type2"` (normalised Type II partial sums of
squares). `"shapley"` is an alias for `"lmg"`.}

\item{residual}{A boolean indicating whether to append a `"Residual"` row for
the unexplained (and choice-interaction) variance. Defaults to
`TRUE`. When `FALSE`, the control percentages are rescaled to
sum to 100.}

\item{title}{A string used as the plot title. Defaults to `""`.}
}
\value{
A ggplot object.
}
\description{
`plot_variance()` visualises [sca_variance()] as a bar chart of the share of
the focal coefficient's variance attributable to each control choice (plus a
`"Residual"` bar), making it easy to see which modelling choices drive the
spread of estimates across the curve.
}
\examples{
s <- sca(y = "Salnty", x = "T_degC",
         controls = c("ChlorA", "O2Sat", "NO2uM"),
         data = bottles, progress_bar = FALSE, parallel = FALSE)
plot_variance(s)
}
\seealso{
[sca_variance()] for the underlying table.
}
