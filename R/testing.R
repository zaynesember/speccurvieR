load("data/bottles.RData")

# Idea: Find variation in coefficient of interest across k-folds, plot variance of coefficient
#       across model specifications
plotCV <- function(folds, y, x, controls, data, family="linear", link=NULL,
                   fixedEffects=NULL, progressBar=TRUE, parallel=FALSE,
                   workers=2){


}
